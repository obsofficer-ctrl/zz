# Standard Library

ZZ ships with a set of standard modules available in the main repository. These modules are stack-based and require no heap allocation.

## Available Modules

### `buffer`

A safe, stack-based byte buffer with append, format, slice, and search operations.

**Source:** `modules/buffer/`

**Usage:**

