# Assertions & Invariants

ZZ provides several annotation keywords to guide the formal verifier.

## `@requires`

Declares a **precondition** — a property that must be true when the function is called.

